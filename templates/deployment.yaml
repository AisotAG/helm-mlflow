apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "aisot-is-market-data-warehouse-service.name" . }}
  labels:
    {{- include "aisot-is-market-data-warehouse-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "aisot-is-market-data-warehouse-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "aisot-is-market-data-warehouse-service.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "aisot-is-market-data-warehouse-service.name" . }}
      imagePullSecrets:
        - name: {{ .Values.imagePullSecret }}
      containers:
        - name: {{ .Chart.Name }}
          image: "ghcr.io/aisotag/docker/mlflow:{{ .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          env:
            - name: BACKEND_STORE_URI
              valueFrom:
                secretKeyRef:
                  name: {{ include "mlflow.name" . }}
                  key: BACKEND_STORE_URI
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - mountPath: /mnt/aksshare/
              name: artifact-store
      volumes:
        - name: artifact-store
          nfs:
            server: {{ .Values.sharedNfs.host }}
            path: {{ .Values.sharedNfs.path }}